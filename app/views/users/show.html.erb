<aside class="sidebar">  
  <section>
    <h3 class="user-info">
      <%= image_tag @user.image_url, width:120 %>
    </h3>
  </section>
  <section>
    <span class="label">アカウント名</span>
    <h3 class="user-info"><%= @user.name %></h3>
  </section>
  
  <section>
    <span class="label">E-mail</span>
    <h3 class="user-info"><%= @user.email %></h3>
  </section>

  <%= link_to @user.likes.count.to_s + "個", like_notes_user_path(@user.id) %>の投稿にいいね！しています

  <% if current_user?(@user) %>
    <section>
      <small><%= link_to 'プロフィールを編集', edit_user_path(@user) %></small>
    </section>
  <% end %>

</aside>

<ul class="notes">
  <%= render @notes %>
</ul>
